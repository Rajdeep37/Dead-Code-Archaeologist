# Dead-Code-Archaeologist

Dead Code Archaeologist is split into two applications:

- `frontend`: React client for browsing analysis results.
- `backend`: Python service for git mining, static analysis, LLM reasoning, and report generation.

## Project Structure

```text
frontend/
backend/
```